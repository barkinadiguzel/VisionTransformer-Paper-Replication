# VisionTransformer-Paper-Replication
This project replicates the Vision Transformer as described in the paper, including patch extraction, patch embeddings, positional encodings, class token, encoder layers, and assembling the full ViT model, following the paper’s structure and formulas.
